# React Vite Source Snapshot

React applications should split feature-owned screens, hooks, state, validation, and presentational components. Shared UI should not import feature code. UI components should not perform database access directly.
